source 'https://rubygems.org'

ruby '3.2.3'

# Bundle edge Rails instead: gem "rails", github: "rails/rails", branch: "main"
gem 'rails', '~> 7.1.3', '>= 7.1.3.2'

# The original asset pipeline for Rails [https://github.com/rails/sprockets-rails]
gem 'sprockets-rails'

# Use postgresql as the database for Active Record
gem 'pg', '~> 1.1'

# Use the Puma web server [https://github.com/puma/puma]
gem 'puma', '>= 5.0'

# Use JavaScript with ESM import maps [https://github.com/rails/importmap-rails]
gem 'importmap-rails'

# Build JSON APIs with ease [https://github.com/rails/jbuilder]
gem 'jbuilder'

# Use Redis adapter to run Action Cable in production
gem 'redis', '>= 4.0.1'

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem 'tzinfo-data', platforms: %i[windows jruby]

# Reduces boot times through caching; required in config/boot.rb
gem 'bootsnap', require: false

# Flexible authentication solution for Rails with Warden [https://rubydoc.info/github/heartcombo/devise]
gem 'devise', '~> 4.9', '>= 4.9.3'

# Simple, efficient background processing for Ruby. [https://github.com/sidekiq/sidekiq/wiki]
# gem 'sidekiq', '~> 7.1', '>= 7.1.2'

# Simple authorization solution for Rails. All permissions are stored in a single location. [https://github.com/CanCanCommunity/cancancan]
# gem 'cancancan', '~> 3.5'

group :development, :test do
  # Ruby debugger [https://www.rubydoc.info/gems/byebug/11.1.3]
  gem 'byebug', '~> 11.1', '>= 11.1.3'

  # RuboCop is a Ruby code style checking and code formatting tool. It aims to enforce the community-driven Ruby Style Guide. [https://docs.rubocop.org/rubocop/1.62/]
  gem 'rubocop', '~> 1.62', '>= 1.62.1'
end

group :development do
  gem 'annotate', '~> 3.2'
end

group :test do
  # rspec is a testing framework for RoR [https://rspec.info/documentation]
  gem 'rspec', '~> 3.4'
end
